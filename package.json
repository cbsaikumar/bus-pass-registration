{
  "name": "node-api",
  "version": "1.0.0",
  "description": "Server side API layer to connect UI(React) with Mongo db",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "cd frontend&&yarn build",
    "start": "npx yarn build&&node index.js",
    "pm2": "pm2 start index.js -i max --watch"
  },
  "dependencies": {
    "@joi/date": "^2.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "joi": "^17.6.4",
    "mongoose": "^6.6.7",
    "nodemon": "^2.0.20"
  }
}
